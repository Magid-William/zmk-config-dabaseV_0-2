manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: maged
      url-base: https://github.com/Magid-William
    - name: badjeff
      url-base: https://github.com/badjeff
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: ac7f75b8
      import: app/west.yml

    # Exp70: direct PS/2 on the right half (gpio-ps2 backend,
    # NO_HOST_COMMANDS). Driver Exp70 = +4096 cb-workqueue stack (right-half
    # hardfault on TP touch fixed) + PS2_GPIO_NO_RESEND (no 0xFE writes).
    - name: zmk-ps2-trackpoint-driver
      remote: maged
      revision: d971d8d

    - name: zmk-behavior-battery-percentage-printer
      remote: badjeff
      revision: main
  self:
    path: config